# ==================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO - S_CONTABLE
# ==================================================
# Copia este archivo como .env y configura tus valores reales
# NUNCA subas el archivo .env al repositorio (ya está en .gitignore)

# ==================================================
# 🔐 SEGURIDAD - DJANGO
# ==================================================

# SECRET_KEY: Clave secreta de Django (OBLIGATORIA)
# Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=tu-clave-secreta-super-larga-y-aleatoria-generada-con-el-comando-de-arriba

# DEBUG: Modo debug (True solo en desarrollo local, False en producción)
DEBUG=True

# ALLOWED_HOSTS: Dominios permitidos (separados por coma, sin espacios)
# Ejemplo desarrollo: 127.0.0.1,localhost
# Ejemplo producción: tudominio.com,*.render.com
ALLOWED_HOSTS=127.0.0.1,localhost

# ==================================================
# 🗄️ BASE DE DATOS
# ==================================================

# DATABASE_URL: URL de conexión a PostgreSQL (dejar vacío para usar SQLite en desarrollo)
# Formato: postgresql://usuario:password@host:puerto/nombre_bd
# Ejemplo Neon: postgresql://usuario:password@ep-xxx.region.neon.tech/dbname?sslmode=require
DATABASE_URL=

# ==================================================
# 📧 EMAIL - SMTP GMAIL
# ==================================================

# Configuración para envío de emails (activación de cuenta, recuperación de contraseña)
# Para obtener EMAIL_HOST_PASSWORD (App Password de Gmail):
# 1. Ir a: https://myaccount.google.com/security
# 2. Habilitar verificación en 2 pasos
# 3. Buscar "Contraseñas de aplicaciones"
# 4. Crear una nueva para "S_CONTABLE Django"
# 5. Copiar la clave de 16 caracteres (sin espacios)

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password-de-16-caracteres
