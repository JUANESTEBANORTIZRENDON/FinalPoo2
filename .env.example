# ==================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO - S_CONTABLE
# ==================================================
# ⚠️  INSTRUCCIONES:
# 1. Copia este archivo como .env (en la raíz del proyecto)
# 2. Reemplaza los valores de ejemplo con tus valores reales
# 3. NUNCA subas el archivo .env al repositorio (ya está en .gitignore)

# ==================================================
# 🔐 SEGURIDAD - DJANGO
# ==================================================

# SECRET_KEY: Clave secreta de Django (OBLIGATORIA)
# Genera una nueva con: python generate_secret_key.py
SECRET_KEY=tu-clave-secreta-generada-con-el-script

# DEBUG: Modo debug (True solo en desarrollo local, False en producción)
DEBUG=True

# ALLOWED_HOSTS: Dominios permitidos (separados por coma, sin espacios)
ALLOWED_HOSTS=127.0.0.1,localhost

# DJANGO_DEV_PASSWORD: Contraseña adicional para acceder al panel de Django Admin
# Esta es una capa extra de seguridad. Genera una contraseña fuerte.
# Ejemplo: python -c "import secrets; print(secrets.token_urlsafe(32))"
DJANGO_DEV_PASSWORD=tu-password-segura-de-desarrollador

# ==================================================
# 🗄️ BASE DE DATOS - POSTGRESQL
# ==================================================

# DATABASE_URL: URL de conexión a PostgreSQL (Neon, Railway, etc.)
# Formato: postgresql://usuario:password@host:puerto/nombre_bd?sslmode=require
# Dejar vacío para usar SQLite en desarrollo local
DATABASE_URL=

# ==================================================
# 📧 EMAIL - GMAIL SMTP
# ==================================================

# Configuración para envío de emails (activación, recuperación de contraseña)
# Para obtener EMAIL_HOST_PASSWORD (App Password de Gmail):
# 1. Ir a: https://myaccount.google.com/security
# 2. Habilitar verificación en 2 pasos
# 3. Buscar "Contraseñas de aplicaciones"
# 4. Crear una nueva para "S_CONTABLE Django"
# 5. Copiar la clave de 16 caracteres (sin espacios)

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password-de-16-caracteres
